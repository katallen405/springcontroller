# Every currently-contributing spring's force/moment/torque, published on
# ~/spring_forces. A spring not currently contributing (disabled, or hasn't
# computed a state yet) is omitted rather than reported as zero -- same
# convention as the JSON payload this replaces.

SpringForce[] springs
